{
  "name": "algolia-search",
  "version": "1.0.0",
  "description": "\"implemention of algolia search with express and mongodb\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node server.js"
  },
  "author": "Inayat ullah",
  "license": "ISC",
  "dependencies": {
    "algoliasearch": "^4.0.3",
    "async": "^3.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "mongoose": "^5.8.9"
  }
}
